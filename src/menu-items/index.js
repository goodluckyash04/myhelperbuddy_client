import dashboard from './dashboard';
import utilities from './utilities';
import preferences from './preferences';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
  items: [dashboard, utilities, preferences]
};

export default menuItems;
